# Permissions-Policy. Suppressed per-location when
# useUpstreamPermissionsPolicy=true so the upstream can set its own policy.
add_header Permissions-Policy "geolocation=(self), microphone=(self), camera=(self), payment=(self), usb=(self)" always;
