services:
  nginx:
    image: nginx
    user: 0:0
    restart: unless-stopped
    ports:
      - "${NGINX_HTTP_PORT}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro,Z
      - ./conf.d:/etc/nginx/conf.d:ro,Z
